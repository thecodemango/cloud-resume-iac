name: Front end workflow

on:
    push:
        branches: [ master ]
        paths:
          - 'cv_IaC_v1/**'

env:
  AWS_REGION : ca-central-1

permissions:
    id-token: write   # This is required for requesting the JWT
    contents: read    # This is required for actions/checkout

jobs:
    FrontEndUpdate:
        runs-on: ubuntu-latest

        steps:
            - name: Git clone the repository
              uses: actions/checkout@v3
        
            - name: Configure AWS credentials
              uses: aws-actions/configure-aws-credentials@v1.7.0
              with:
                  role-to-assume: arn:aws:iam::${{ secrets.AWS_ACCOUNT_ID }}:role/GitHubAction-AssumeRoleWithAction 
                  role-session-name: GitHub_to_AWS_via_FederatedOIDC
                  aws-region: ${{ env.AWS_REGION }}
            
            - uses: actions/setup-python@v5
              with:
                python-version: '3.13'
                cache: 'pip' # caching pip dependencies
            - run: pip3 install awscli --upgrade --user 
           # - name: Install AWS CLI
           #   uses: ItsKarma/aws-cli@v1.70.0
           #   with:
           #     args: s3 cp cv_IaC_v1 s3://thecodemango-cv/upload_test --recursive --exclude ".*" #TODO find a way to just upload changed file